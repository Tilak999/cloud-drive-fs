{
    "name": "@ideabox/cloud-drive-fs",
    "version": "0.3.0",
    "description": "Cloud Storage using service account",
    "main": "build/GdriveFS.js",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ideaboxx/cloud-drive-fs.git"
    },
    "scripts": {
        "build": "npm i && npx tsc",
        "push": "npm run build && npm publish --access public"
    },
    "author": "Tilak999",
    "devDependencies": {
        "@types/node": "^20.11.10",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "googleapis": "^131.0.0"
    }
}
